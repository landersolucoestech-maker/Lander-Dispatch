import { Link } from "wouter";
import { CalendarClock, CheckCircle2, ClipboardList, FileEdit, Gauge, ListChecks, Megaphone, Plus, Rocket, Sparkles } from "lucide-react";
import { Button } from "@/shared/components/ui/button";
import { MarketingKpiCard, MarketingSectionCard, StatusBadge } from "../components/MarketingPrimitives";
import { useMarketingState } from "../state";

const QUICK=[{href:"/marketing/campaigns",label:"New Campaign",icon:Megaphone},{href:"/marketing/calendar",label:"New Content",icon:CalendarClock},{href:"/marketing/tasks",label:"New Task",icon:ListChecks},{href:"/marketing/ai-creative",label:"AI Creative",icon:Sparkles}];
export default function MarketingOverviewPage(){const[state]=useMarketingState();const activeCampaigns=state.campaigns.filter(c=>c.status==="Active").length;const scheduled=state.contents.filter(c=>c.status==="Scheduled").length;const pendingTasks=0;const published=state.contents.filter(c=>c.status==="Published").length;const pendingApprovals=state.contents.filter(c=>c.approval==="Pending").length;const upcoming=state.contents.filter(c=>c.publishDate&&new Date(`${c.publishDate}T${c.publishTime||"00:00"}`)>=new Date()).sort((a,b)=>a.publishDate.localeCompare(b.publishDate)).slice(0,6);const performance=state.campaigns.length?Math.round(state.campaigns.reduce((s,c)=>s+Math.max(0,Math.min(100,c.metrics.roi||0)),0)/state.campaigns.length):0;return <div className="space-y-6 p-4 sm:p-6 lg:p-8"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><MarketingKpiCard title="Active Campaigns" value={activeCampaigns} icon={Megaphone}/><MarketingKpiCard title="Active Projects" value={state.campaigns.filter(c=>c.status!=="Completed"&&c.status!=="Cancelled").length} icon={Rocket}/><MarketingKpiCard title="Scheduled Content" value={scheduled} icon={CalendarClock}/><MarketingKpiCard title="Pending Tasks" value={pendingTasks} icon={ListChecks}/><MarketingKpiCard title="Published Content" value={published} icon={FileEdit}/><MarketingKpiCard title="Pending Approvals" value={pendingApprovals} icon={CheckCircle2}/><MarketingKpiCard title="Upcoming Deliveries" value={upcoming.length} icon={CalendarClock}/><MarketingKpiCard title="Sector Performance" value={`${performance}%`} icon={Gauge}/></div><div className="grid gap-6 lg:grid-cols-3"><div className="space-y-6 lg:col-span-2"><MarketingSectionCard title="Upcoming Deliveries" description="Content to publish" action={<Link href="/marketing/calendar"><Button variant="ghost" size="sm">View calendar</Button></Link>}>{upcoming.length?<ul className="divide-y divide-slate-200">{upcoming.map(content=><li key={content.id} className="flex items-center justify-between gap-3 py-3"><div className="min-w-0"><p className="truncate text-sm font-medium">{content.title}</p><p className="text-[11px] text-slate-500">{content.channel} · {content.publishDate} {content.publishTime}</p></div><StatusBadge value={content.status}/></li>)}</ul>:<p className="py-4 text-sm text-slate-500">Nothing scheduled.</p>}</MarketingSectionCard><MarketingSectionCard title="Pending Approvals" description="Content waiting for approval">{state.contents.filter(c=>c.approval==="Pending").length?<ul className="divide-y divide-slate-200">{state.contents.filter(c=>c.approval==="Pending").slice(0,6).map(c=><li key={c.id} className="flex items-center justify-between py-3"><div><p className="text-sm font-medium">{c.title}</p><p className="text-[11px] text-slate-500">{c.owner||"Unassigned"}</p></div><StatusBadge value={c.approval}/></li>)}</ul>:<p className="py-4 text-sm text-slate-500">No pending approvals.</p>}</MarketingSectionCard></div><div className="space-y-6"><MarketingSectionCard title="Quick Actions"><div className="space-y-2">{QUICK.map(({href,label,icon:Icon})=><Link key={href} href={href}><Button variant="outline" size="sm" className="w-full justify-start gap-2"><Plus className="h-3.5 w-3.5"/><Icon className="h-3.5 w-3.5"/>{label}</Button></Link>)}</div></MarketingSectionCard><MarketingSectionCard title="Recent Activity"><div className="space-y-3">{[...state.campaigns.slice(-2).map(c=>({id:c.id,label:`Campaign: ${c.name}`,meta:c.status})),...state.contents.slice(-3).map(c=>({id:c.id,label:`Content: ${c.title}`,meta:c.status}))].reverse().map(item=><div key={item.id} className="flex gap-3"><ClipboardList className="mt-0.5 h-4 w-4 text-[#1E3D7A]"/><div><p className="text-xs font-medium">{item.label}</p><p className="text-[11px] text-slate-500">{item.meta}</p></div></div>)}</div></MarketingSectionCard></div></div></div>}
